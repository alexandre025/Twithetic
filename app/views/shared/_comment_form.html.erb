<% if user_signed_in? %>
    <%= form_for [post, Comment.new] do |f| %>
        <%= f.hidden_field :user_id, value: current_user.id %>
        <%= f.text_field :message %>
        <%= f.submit :submit %>
    <% end %>
<% end %>